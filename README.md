awk-gmap-height-profile
=======================

Accumulates the height profile from http://geo.ebp.ch/gelaendeprofil/ with awk.

usage: awk -f height.awk [CSV height data]

one example csv included

